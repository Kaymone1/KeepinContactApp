<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>
<body>
    <h1>New Contact Page</h1>
    <form action="/contacts" method="POST">
        Name: <input type="text" name="name" value="<%= contact.name %>" placeholder="Name">
        Phone Number: <input type="text" name="number" value="<%= contact.number %>" placeholder="Number">
        Email: <input type="text" name="email" value="<%= contact.email %>" placeholder="Email">
        Category:
        <select name="category">
            <option value="Family">Family</option>
            <option value="Friend">Friend</option>
            <option value="Business">Business</option>
        </select>
        Image URL: <input type="text" name="imageUrl" value="<%= contact.imageUrl %>" placeholder="Image URL">
        Comment: <textarea name="comment" placeholder="Comment"><%= contact.comment %></textarea>
        <input type="submit" value="Create New Contact"/>
    </form>
</body>
</html>